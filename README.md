# Darwin Worklist PACS 1.4.2

Source baseline for Darwin2 Worklist / PACS bridge.

Version: 1.4.2

Build with `Build-EXE-Select-Icon.bat`. The builder allows selecting an `.ico` file and falls back to `assets/Darwin2WorklistBridge.ico` when cancelled.
